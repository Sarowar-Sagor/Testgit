#include<stdio.h>
#include<conio.h>
int main()
{
    float length,width,area;
    printf("Enter the length and width: ");
    scanf("%f %f",&length,&width);
    area=length*width;
    printf("area of the rectangular: %f",area);
    return 0;




}
